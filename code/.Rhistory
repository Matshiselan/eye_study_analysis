group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ fixations_possibly(.x))) %>%
# Pivot and clean the resulting statistics
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup()
subject_screen_visit_fixations
subject_screen_visit_fixations %>%
filter(!is.na(x)) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count()
# fixations function
fixation_trial <- function(subject_data) {
#' This function detects fixations in eye-tracking data using the provided subject data.
#'
#' @param
#' ------
#' subject_data: A data frame containing eye-tracking data for a specific subject.
#'
#' @return
#' -------
#'fixations: A data frame representing fixations detected in the eye-tracking data.
#'
#' @examples
#' ---------
#' # fixation_data <- fixation_trial(my_subject_data)
#'
# Detect fixations using external function detect.fixations
# Parameters:
# - subject_data: Eye-tracking data for the subject
# - smooth.coordinates: Perform smoothing on eye coordinates (default: TRUE)
# - smooth.saccades: Perform smoothing on saccade data (default: TRUE)
fixations <- detect.fixations(subject_data,
smooth.coordinates = TRUE,
smooth.saccades = F)
fixations
}
# Wrapper function for possibly handling errors in fixation_trial
fixations_possibly <- possibly(fixation_trial, otherwise = NA)
subject_screen_visit_fixations %>%
filter(!is.na(x)) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count()
subject_screen_visit_fixations <- subject_data_fin %>%
select(-value) %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ fixations_possibly(.x))) %>%
# Pivot and clean the resulting statistics
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup()
# Plot to see discontinuities in the data
ggplot(data = subject_72_5_5,
aes(x = time)) + # define time series x axis
geom_line(aes(y = x, colour = 'X')) +  # plot horizontal data
geom_line(aes(y = y, colour = 'Y')) + # plot vertical time series too
scale_color_manual(name = "Coordinates", values = c("X" = "darkblue",
"Y" = "darkred")) +
labs(x = "Time", y = "X and Y lines",
title = "X, Y coordinates by Time") +
theme_bw()
#' Calculate screen view statistics for each subject, screen, and visit
#' combination.
screen_view_stats <- subject_data_fin %>%
# Group by subject, screen_num, and num_visit_screen
group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ process_possibly(.x))) %>%
# Pivot and clean the resulting statistics
mutate(stats = map(stats, ~ pivot_wider_and_clean(.x))) %>%
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup() %>%
# Rename columns with trial to screen_visit
rename_with(~str_replace(., "trial", "screen_visit"), contains("trial")) %>%
# Select specific columns for the final output
select(subject, screen_num, num_visit_screen, mean_no_of_fixations_per_screen_visit,
mean_duration_of_fixations) %>%
# This column will help differentiate when we combina the fixations stats
mutate(data = "subject_screen_visit")
# View output
#screen_view_stats %>% head(100) %>% view()
# Save screen_view_stats_output as csv file
write_csv(screen_view_stats,
'subject_data/screen_view_stats_output_issue.csv')
screen_stats_output %>%
filter(subject==54) %>%
count(screen_num)
screen_view_stats %>%
filter(subject==54) %>%
count(screen_num)
screen_view_stats %>%
filter(subject == 54, screen_num ==3)
screen_view_stats %>%
filter(subject==54) %>%
count(screen_num)
screen_view_stats %>%
filter(subject == 54, screen_num ==3)
# filter(subject %in% c(54, 72))
# Data for subject 54, screen number 2, visit ==4
subject_data_fin %>%
filter(subject == 54, screen_num ==2, num_visit_screen ==1)
# Fixations for subject 54, screen number 2, visit ==4
subject_data_fin %>%
filter(subject == 54, screen_num ==2, num_visit_screen ==4) %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ fixations_possibly(.x))) %>%
# Pivot and clean the resulting statistics
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup()
# Data for subject 72, screen number 5, visit ==5
subject_72_5_5 <- subject_data_fin %>%
filter(subject == 72, screen_num ==5, num_visit_screen ==5) %>%
select(time, x, y, trial)
# Fixations for subject 72, screen number 5, visit ==5
subject_72_5_5_fix <- subject_data_fin %>%
filter(subject == 72, screen_num ==5, num_visit_screen ==5) %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ fixations_possibly(.x))) %>%
# Pivot and clean the resulting statistics
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup()
# Fixations
subject_72_5_5_fix
# Plot to see discontinuities in the data
ggplot(data = subject_72_5_5,
aes(x = time)) + # define time series x axis
geom_line(aes(y = x, colour = 'X')) +  # plot horizontal data
geom_line(aes(y = y, colour = 'Y')) + # plot vertical time series too
scale_color_manual(name = "Coordinates", values = c("X" = "darkblue",
"Y" = "darkred")) +
labs(x = "Time", y = "X and Y lines",
title = "X, Y coordinates by Time") +
theme_bw()
# Fixations for subject 72, screen number 5, visit ==5
subject_data_fin %>%
filter(subject == 72, screen_num ==2, num_visit_screen ==3) %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ fixations_possibly(.x))) %>%
# Pivot and clean the resulting statistics
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup()
subject_screen_visit_fixations <- subject_data_fin %>%
select(-value) %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ fixations_possibly(.x))) %>%
# Pivot and clean the resulting statistics
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup()
subject_screen_visit_fixations
subject_screen_visit_fixations %>%
filter(!is.na(x)) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count()
subject_screen_visit_fixations %>%
filter(is.na(x)) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count()
write_csv(subject_screen_visit_fixations,
"subject_data/subject_screen_visit_fixations.csv")
subject_data_nw <- subject_data_fin %>%  # combine the data sets
filter(!(screen_num %in% c(0, "0"))) %>%   # remove screen number 0
mutate(treatment = case_when(
subject %in% 1:33 ~ 1,
subject %in% 34:66 ~ 2,
subject %in% 67:96 ~ 3,
subject %in% 97:128 ~ 4,
subject %in% 129:160 ~ 5,
subject %in% 161:193 ~ 6,
subject %in% 194:226 ~ 7,
subject %in% c(88, 114, 248) ~ 0,
TRUE ~ 8 # Handle other cases if needed
)) %>%
# Calculate groups based on treatment
mutate(groups = case_when(treatment %in% 1:4 ~ "group 1",
T ~ "group 2")) %>%
# mutate(groups = case_when(treatment %in% 1:4 ~ "group 1",
#                         T ~ "group 2")) %>%
# Calculate selection based on groups and screen number conditions
mutate(selection = case_when(groups == "group 1" &
screen_num == 8 ~ 1, #screen num 8 & Teatment 1-4
groups == "group 2" &
screen_num == 10 ~ 1, #screen num 10 & Teatment 5-8
T ~ 0)) %>%
# Filter rows based on selection
filter(selection != 1) %>% # remove selection
# Remove the temporary selection column
select(-selection)
#' Calculate screen view statistics for each subject, screen, and visit
#' combination.
screen_view_stats <- subject_data_fin %>%
# Group by subject, screen_num, and num_visit_screen
group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ process_possibly(.x))) %>%
# Pivot and clean the resulting statistics
mutate(stats = map(stats, ~ pivot_wider_and_clean(.x))) %>%
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup() %>%
# Rename columns with trial to screen_visit
rename_with(~str_replace(., "trial", "screen_visit"), contains("trial")) %>%
# Select specific columns for the final output
select(subject, screen_num, num_visit_screen, mean_no_of_fixations_per_screen_visit,
mean_duration_of_fixations) %>%
# This column will help differentiate when we combina the fixations stats
mutate(data = "subject_screen_visit")
# View output
#screen_view_stats %>% head(100) %>% view()
# Save screen_view_stats_output as csv file
write_csv(screen_view_stats,
'subject_data/screen_view_stats_output_issue.csv')
# fixations function
fixation_trial <- function(subject_data) {
#' This function detects fixations in eye-tracking data using the provided subject data.
#'
#' @param
#' ------
#' subject_data: A data frame containing eye-tracking data for a specific subject.
#'
#' @return
#' -------
#'fixations: A data frame representing fixations detected in the eye-tracking data.
#'
#' @examples
#' ---------
#' # fixation_data <- fixation_trial(my_subject_data)
#'
# Detect fixations using external function detect.fixations
# Parameters:
# - subject_data: Eye-tracking data for the subject
# - smooth.coordinates: Perform smoothing on eye coordinates (default: TRUE)
# - smooth.saccades: Perform smoothing on saccade data (default: TRUE)
fixations <- detect.fixations(subject_data,
smooth.coordinates = TRUE,
smooth.saccades = T)
fixations
}
# Wrapper function for possibly handling errors in fixation_trial
fixations_possibly <- possibly(fixation_trial, otherwise = NA)
# fixations function
fixation_trial <- function(subject_data) {
#' This function detects fixations in eye-tracking data using the provided subject data.
#'
#' @param
#' ------
#' subject_data: A data frame containing eye-tracking data for a specific subject.
#'
#' @return
#' -------
#'fixations: A data frame representing fixations detected in the eye-tracking data.
#'
#' @examples
#' ---------
#' # fixation_data <- fixation_trial(my_subject_data)
#'
# Detect fixations using external function detect.fixations
# Parameters:
# - subject_data: Eye-tracking data for the subject
# - smooth.coordinates: Perform smoothing on eye coordinates (default: TRUE)
# - smooth.saccades: Perform smoothing on saccade data (default: TRUE)
fixations <- detect.fixations(subject_data,
smooth.coordinates = TRUE,
smooth.saccades = F)
fixations
}
# Wrapper function for possibly handling errors in fixation_trial
fixations_possibly <- possibly(fixation_trial, otherwise = NA)
gc()
library(tidyverse) # package for data manipulation and visualization.
library(janitor) # package for cleaning and tidying data.
# library("devtools")
# install_github("tmalsburg/saccades/saccades",
# dependencies=TRUE)
library(saccades) # package for analyzing eye movement data.
subject_data_fin <- read_csv("subject_data/subject_data.csv")
# Load external functions from "functions.R"
source("functions.R")
subject_data_fin <- read_csv("subject_data/subject_data.csv") %>%
filter(!(screen_num %in% c(0, "0")))   # remove screen number 0
subject_72_5_5 <- subject_data_fin %>%
filter(subject == 72, screen_num ==5, num_visit_screen ==5) %>%
select(time, x, y, trial)
# Fixations for subject 72, screen number 5, visit ==5
subject_72_5_5_fix <- subject_data_fin %>%
filter(subject == 72, screen_num ==5, num_visit_screen ==5) %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ fixations_possibly(.x))) %>%
# Pivot and clean the resulting statistics
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup()
# Fixations
subject_72_5_5_fix
# Plot to see discontinuities in the data
ggplot(data = subject_72_5_5,
aes(x = time)) + # define time series x axis
geom_line(aes(y = x, colour = 'X')) +  # plot horizontal data
geom_line(aes(y = y, colour = 'Y')) + # plot vertical time series too
scale_color_manual(name = "Coordinates", values = c("X" = "darkblue",
"Y" = "darkred")) +
labs(x = "Time", y = "X and Y lines",
title = "X, Y coordinates by Time") +
theme_bw()
()
subject_screen_visit_fixations <- subject_data_fin %>%
select(-value) %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ fixations_possibly(.x))) %>%
# Pivot and clean the resulting statistics
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup()
subject_screen_visit_fixations
subject_screen_visit_fixations %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count(event)
subject_screen_visit_fixations %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count(event) %>%
mutate(percentage = n/sum(n))
subject_screen_visit_fixations %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count(event) %>%
mutate(percentage = scales::percent(n/sum(n)))
write_csv(subject_screen_visit_fixations_count,
"output/subject_screen_visit_fixations_count.csv")
subject_screen_visit_fixations_count <- subject_screen_visit_fixations %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count(event) %>%
mutate(percentage = scales::percent(n/sum(n)))
write_csv(subject_screen_visit_fixations_count,
"output/subject_screen_visit_fixations_count.csv")
subject_screen_visit_fixations %>%
filter(!is.na(x))
nrow()
nrow(subject_screen_visit_fixations)
subject_screen_visit_fixations %>%
filter(!is.na(x)) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count()
nrow(subject_screen_visit_fixations %>%
# filter(!is.na(x)) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count()
)
total_non_missing_rows <- nrow(subject_screen_visit_fixations %>%
filter(!is.na(x)) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count()
)
total_non_missing_rows
non_missing_perc <- total_non_missing_rows/total_rows
non_missing_perc
total_rows <- nrow(subject_screen_visit_fixations %>%
# filter(!is.na(x)) %>%
group_by(subject, screen_num, num_visit_screen) %>%
count()
)
non_missing_perc <- total_non_missing_rows/total_rows
non_missing_perc
glue::glue("The total percentage of data that is missing is {non_missing_perc}")
non_missing_perc <- (total_non_missing_rows/total_rows)*100
glue::glue("The total percentage of data that is missing is {non_missing_perc}")
non_missing_perc <- round((total_non_missing_rows/total_rows)*100,2)
glue::glue("The total percentage of data that is missing is {non_missing_perc}")
glue::glue("The total percentage of data that is missing is {1-non_missing_perc}%")
missing_perc <- 1 - non_missing_perc
glue::glue("The total percentage of data that is missing is {missing_perc}%")
missing_perc <- 1 - non_missing_perc
missing_perc
missing_perc <- 1 - as.numeric(non_missing_perc)
missing_perc
as.numeric(non_missing_perc)
missing_perc <- 100 - as.numeric(non_missing_perc)
missing_perc
glue::glue("The total percentage of data that is missing is {missing_perc}%")
subject_screen_visit_fixations %>%
head()
subject_screen_visit_fixations_count %>%
head()
subject_screen_fixations <- subject_data_fin %>%
select(-value) %>%
# group_by(subject, screen_num) %>%
group_by(subject, screen_num) %>%
# Nest data for each group
nest(data = c(treatment, x, y, time, trial)) %>%
# Calculate statistics for nested data using process_possibly
mutate(stats = map(data, ~ fixations_possibly(.x))) %>%
# Pivot and clean the resulting statistics
# Unnest the statistics
unnest(stats) %>%
# Select relevant columns and remove the nested data column
select(-data) %>%
# Ungroup the data
ungroup()
subject_screen_fixations
# Save the fixations data
write_csv(subject_screen_fixations,
"subject_data/subject_screen_fixations.csv")
# Get the first 6 rows output
subject_screen_fixations %>%
head()
# Count the events (fixations, blinks etc)
subject_screen_fixations_count <- subject_screen_fixations %>%
# group_by(subject, screen_num, num_visit_screen) %>%
group_by(subject, screen_num) %>%
count(event) %>%
mutate(percentage = scales::percent(n/sum(n)))
subject_screen_fixations_count
subject_screen_visit_fixations_count
# Take a look at the first 6 rows
subject_screen_fixations_count %>%
head()
# Non missing information
total_non_missing_rows_2 <- nrow(subject_screen_fixations %>%
filter(!is.na(x)) %>%
group_by(subject, screen_num) %>%
count()
)
total_non_missing_rows_2
# Total rows of info
total_rows_2 <- nrow(subject_screen_fixations %>%
# filter(!is.na(x)) %>%
group_by(subject, screen_num) %>%
count()
)
total_rows_2
#percentage
non_missing_perc_2 <- round((total_non_missing_rows_2/total_rows_2)*100,2)
missing_perc_2 <- 100 - as.numeric(non_missing_perc_2)
# Percentage report
glue::glue("The total percentage of data that is missing is {missing_perc_2}%, meaning we have a total of {non_missing_perc_2}% non missing data points")
missing_perc_2
non_missing_perc_2
missing_perc_2 <- round(100 - as.numeric(non_missing_perc_2),2)
missing_perc_2
# Percentage report
glue::glue("The total percentage of data that is missing is {missing_perc_2}%, meaning we have a total of {non_missing_perc_2}% non missing data points")
